

## Plan: Update Copyright Year

Single change in `src/components/HeartbeatFooter.tsx` (line 37): update `2025` to `2026`. Since this shared footer component is used across all pages, the change propagates everywhere automatically.

