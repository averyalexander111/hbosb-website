

## Prevent "AI Systems for Modern Businesses." Text Wrapping

Only one instance found in `src/components/HeartbeatFooter.tsx` (line 22-24).

### Change

Wrap the tagline text in a `whitespace-nowrap` class to keep it on a single line:

```tsx
<p className="text-sm text-navy-foreground/60 whitespace-nowrap">
  AI Systems for Modern Businesses.
</p>
```

Single-line change, one file.

