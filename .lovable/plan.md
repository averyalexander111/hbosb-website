

## Refine Hero Section Copy

**File:** `src/components/HeartbeatHero.tsx`

### Copy Changes (text only, no layout/structure changes)

1. **Headline (line 94):** Keep as-is — "AI Revenue Systems for Modern Businesses" aligns with brand positioning.

2. **Primary subhead (line 103):** Replace with:
   `"We build AI-powered systems that help service-based businesses capture leads, automate follow-up, and convert more opportunities into revenue."`
   — Adds audience specificity ("service-based businesses") and broadens beyond voice agents.

3. **Secondary subhead (line 112):** Replace with:
   `"From instant lead response to outbound reactivation — every step of your pipeline, handled by intelligent systems."`
   — Reflects full scope (lead capture, follow-up, outbound, reactivation) without narrowing to voice agents.

4. **Benefit pills (lines 123-125):** Update to:
   - `"Capture every lead"` (with Zap icon)
   - `"Automate follow-up"` (with Bot icon)  
   - `"Grow revenue"` (with TrendingUp icon)
   — More action-oriented and covers the full funnel.

No changes to layout, animations, CTAs, or design.

