Remove the second paragraph ("Depending on the business...") from the new CTA card in `src/components/AISystemsSection.tsx`. Update the remaining body paragraph's bottom margin from `mb-4` to `mb-8` to preserve the existing spacing above the button. No other changes.