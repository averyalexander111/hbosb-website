

## Update ResultsSection Text Content

Text-only updates to `src/components/ResultsSection.tsx`. No layout or structure changes.

### Changes

1. **Headline** (line 27): Replace `"Real Results From AI Systems"` with `"How AI Systems Improve Business Performance"`

2. **Metric labels** (lines 5-7): Update the `label` values:
   - "Reduce no-shows" → "Reduction in missed appointments through automated reminders"
   - "Improve client retention" → "Improvement in client retention with consistent follow-up"
   - "Increase booking conversion" → "Increase in booking conversion from faster response times"

3. **Testimonial quote** (lines 56-59): Replace with: `"Heartbeat of South Bay helped us automate our follow-up process and respond to leads faster. We started seeing more consistent bookings and fewer missed opportunities."`

4. **Attribution** (line 60): Change `"— Satisfied Client"` to `"— Client"`

No changes to layout, spacing, typography, icons, or animations.

